"""Phase 0 feasibility spike: preprocess → OCR → VLM extract → validate → score.

Throwaway-adjacent: the OCR/extract/validate components are written to be lifted
into the real pipeline package in Phase 1 (plan.md §10).
"""
